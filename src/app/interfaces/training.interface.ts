export interface TrainingInterface {
  title: string;
  description: string;
  online: number;
  deadline: string;
  departments: number[];
  employees: number[];
  forDepartments: boolean;
  forEmployees: boolean;
}
